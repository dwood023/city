# Pin the toolchain so local builds and CI produce identical results.
# Matches the toolchain this project was developed against (Bevy 0.19
# requires Rust >= 1.95).
[toolchain]
channel = "1.98.0"
components = ["rustfmt", "clippy"]
